[ -d ~/.bash_completion.d ] && {
	for i in `LC_ALL=C command ls ~/.bash_completion.d`; do
		. ~/.bash_completion.d/$i
	done
}
#[ -r ~/.rbenv/completions/rbenv.bash ] && . ~/.rbenv/completions/rbenv.bash
#[ -r ~/.rvm/scripts/completion ] && . ~/.rvm/scripts/completion
